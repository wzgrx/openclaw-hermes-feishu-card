# Security

- Keep Feishu credentials in the host runtime configuration or environment.
- Do not commit `.env`, `openclaw.json`, `config.yaml`, access tokens, app secrets or production card payload logs.
- Rotate a credential immediately after accidental disclosure.
- Report vulnerabilities through a private GitHub security advisory for this repository.
